$(function(){
	//Starting point for when the page loads

  var rows = $("tr");
  //Storing all the rows in an array

  for (i = 0; i < rows.length; i++) {
      rows[i]
  }
  //Iterating through the array, changing every other row colour

});
